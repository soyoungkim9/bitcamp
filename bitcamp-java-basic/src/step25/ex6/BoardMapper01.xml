<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="BoardMapper">
   <!-- SQL문은 태그 안에 작성한다.
     <select>태그에는 select문장을,
     <insert>태그에는 insert문장을,
     <update>태그에는 update문장을,
     <delete>태그에는 delete문장을 작성한다.
            그런데 insert/update/delete인 경우
     <insert>/<update>/<delete> 구분없이 사용해도 된다.
            어차피 SQL문을 찾을 떄 id 속성 값으로 찾기 때문이다.
            그럼에도 불구하고 유지보수의 일관성을 위해
     SQL문의 따라 적절한 태그를 사용하라! 
      ==> 컬렴명과 일치하는 셋터를 호출한다.
                       컬럼명 ==> set컬럼명()
      ==> 예)
          bno ==> setBno(값)
                      즉,
          Board board = new Board();
          board.setBno(rs.getNo("bno"))
     
     
     -->
  
  <!-- select태그
        id: SQL문을 찾을 때 사용할 식별자이다.
        resultType : select 결과를 저장할 클래스이다. 
                  클래스 이름일 경우 반드시 fully-qualified name 을 사용하라! -->
  <select id="selectBoard" resultType="Board">
  select bno,titl,cont,cdt from ex_board
  </select>
</mapper>